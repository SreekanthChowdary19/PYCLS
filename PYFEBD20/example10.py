# WAP count the total number of vowels from the given string



str1 = "hyderabad"


# loop - for
c = 0
for ch in str1:
    if ch == 'a' or ch == 'e' or ch == 'i' or ch == 'o' or ch == 'u':
        c = c+1 # c += 1  # 
print(c)
        
